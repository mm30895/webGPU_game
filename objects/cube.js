
// import { GLTFLoader } from 'engine/loaders/GLTFLoader.js';

// const gltfLoader = new GLTFLoader();
// await gltfLoader.load(new URL('assets/cube.gltf', import.meta.url));

// const cube = gltfLoader.loadNode('Cube');
// //console.log(cube)

// const transform = cube.components[0]; // The first component is the Transform

// // Update the translation property
// transform.translation = [0, 5, 0];
// cube.update = (time, dt) => {
//     // Access the Transform component
//     const transform = cube.components[0];

//     // Example: Move the cube up and down using a sine wave
//     const speed = 2;
//     transform.translation[1] = 10 + Math.sin(time * speed) * 2;

// };

// export { cube };
